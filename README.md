# keyboard
Two-key keyboard

This is an stm32 project using the blue pill to create a keyboard with the intention of using it to play osu so that you don't wear out specific keys on your keyboard when playing.

Use another stm32 with STLink to flash the code through UART/serial debugging since the blue pill doesn't have STLink.
